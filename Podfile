# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GitHub_repositories_search' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GitHub_repositories_search
  pod 'Alamofire', '~> 4.4'

  target 'GitHub_repositories_searchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GitHub_repositories_searchUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
